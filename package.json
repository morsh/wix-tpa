{
  "name": "wix-tpa-monorepo",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "deploy": "yarn build && yarn workspaces run deploy"
  },
  "workspaces": {
    "packages": [
      "packages/*"  
    ],
    "nohoist": ["**/wix-dashboard-iframe-sdk", "**/wix-dashboard-iframe-sdk/**"]
  }
}